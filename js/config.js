/* ===========================================================================
   config.js — ÚNICO ARCHIVO QUE DEBES EDITAR EN EL FRONTEND
   ===========================================================================

   1) API_URL: la dirección de tu backend en Render.
      La copias del panel de Render una vez desplegado. Se ve así:
      https://incidencias-api.onrender.com
      (SIN diagonal al final)

   2) El token NO se escribe aquí: se pide al entrar y se guarda solo en el
      navegador de cada usuaria. Así el código puede ser público en GitHub
      sin exponer la contraseña.
=========================================================================== */

const CONFIG = {
  // 👇 CAMBIA ESTO por la URL de tu servicio en Render
  API_URL: "//https://backend-qp3b.onrender.com",

  // Nombre con el que se guarda el token en el navegador
  CLAVE_TOKEN: "incidencias_token",
};
